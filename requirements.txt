# ============================================================
# Trading System — Python 3.11.6 Requirements
# ============================================================
# CHANGE LOG (from original):
#   - REMOVED ta-lib==0.4.28  → not imported anywhere in the
#     project; requires Microsoft C++ Build Tools on Windows
#     to compile from source. Caused the wheel build failure.
#   - pandas_ta_classic is the ONLY TA library used in code
#     (indicators/indicators.py: `import pandas_ta_classic as ta`)
#   - All other versions pinned to stable releases confirmed
#     compatible with Python 3.11.6 on Windows
# ============================================================

# Core
python-dotenv==1.0.1
pyyaml==6.0.2
numpy==2.1.3
pandas==2.2.2

# Technical Indicators
# ta-lib intentionally omitted — not used anywhere in the codebase.
# All indicators (EMA, RSI, MACD, ATR, Bollinger, SuperTrend, ADX,
# Stochastic, Parabolic SAR, OBV, VWAP) are implemented directly
# in indicators/indicators.py using pandas/numpy.
pandas_ta_classic==0.3.78

# API Clients
aiohttp==3.9.5
requests==2.32.3

# Machine Learning / Optimization
scikit-learn==1.5.1
scipy==1.13.1

# Dashboard
streamlit==1.36.0
plotly==5.22.0
altair==5.3.0

# Notifications
python-telegram-bot==21.4
telethon==1.36.0

# Testing
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0

# Utilities
loguru==0.7.2
tqdm==4.66.5
joblib==1.4.2

